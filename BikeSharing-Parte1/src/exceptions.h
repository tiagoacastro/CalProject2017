#pragma once

#include <string>

using namespace std;
/**
 * Invalid Format exception
 */
class InvalidFormat {
    public:
    /**
     * @brief default constructor for exception InvalidFormat
     */
    InvalidFormat();
};
